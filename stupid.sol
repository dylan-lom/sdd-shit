pragma solidity ^0.4.19;

contract blah {
    bytes32 DateString = "150219";
    uint datePos;
    uint monthPos;
    bytes32 Days;
    bytes32 Month;
    
    
    function getIndex(uint index) public view returns (bytes1) {
        return DateString[index];
    }
    
    function getDatePos(uint pos) public {
        uint datePos = pos;
    }
    function getMonthPos(uint pos) public {
        uint monthPos = pos;
    }
    
    
    for (uint)
}
